# 测试环境配置
database:
  host: localhost
  port: 5432
  user: athena
  password: athena123
  name: athena_test

redis:
  host: localhost
  port: 6379
  password: redis123

llm:
  provider: ollama
  model: claude-sonnet-4-6

gateway:
  log_level: info

agents:
  log_level: info

debug: true
