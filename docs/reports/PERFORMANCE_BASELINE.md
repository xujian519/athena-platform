# Prompt System 主链路 API 性能基线

> 记录 Athena Prompt Engineering 项目核心 API `/api/v1/prompt-system/prompt/generate` 在不同并发压力下的性能表现。

## 测试环境

| 项 | 配置 |
|---|---|
| 服务版本 | `v2.1.0` |
| 部署方式 | Docker Compose（本地基线）/ K8s（生产基线） |
| CPU | 8 vCPU |
| 内存 | 16 GB |
| LLM 后端 | 云端 API（OpenAI / Azure） |
| 数据库 | Neo4j 5.x + PostgreSQL 15 + Redis 7 |
| 缓存状态 | 已预热（Locust `on_start` 自动填充） |
| 测试时间 | 2026-04-23 |
| 测试人员 | Agent-PerfObs |

## 压测任务构成

| 任务类型 | 权重 | 说明 |
|---|---|---|
| OA 解读 (`office_action`) | 50% | 审查意见通知书分析与答复建议 |
| 创造性分析 (`creativity_analysis`) | 30% | 技术方案创造性 / 非显而易见性评估 |
| 新颖性分析 (`novelty_analysis`) | 20% | 权利要求新颖性比对分析 |

## 代码路径耗时分析

基于 `reports/performance_profile.txt` 的 cProfile 统计与代码静态分析，各阶段耗时构成如下（未开启融合）：

| 阶段 | 耗时占比 | 典型延迟 | 瓶颈类型 | 优化方向 |
|---|---|---|---|---|
| 场景识别 (`ScenarioIdentifierOptimized`) | ~35% | 40–120 ms | CPU（BGE-M3 编码） | 本地缓存高频意图 |
| 规则检索 (`ScenarioRuleRetrieverOptimized`) | ~25% | 30–80 ms | IO（Neo4j Cypher） | 热点规则本地缓存（TTL 5 min） |
| 变量治理（校验 + 清洗） | ~15% | 10–30 ms | CPU（regex / schema） | schema 预编译、正则缓存 |
| 模板渲染（Jinja2 / 字符串替换） | ~5% | 2–8 ms | CPU | 结果级缓存已覆盖 |
| 缓存查询 (`PromptTemplateCache.get`) | ~1% | <1 ms | 内存 | — |
| 网络 / 序列化开销 | ~19% | 15–40 ms | IO | HTTP/2、连接池 |

> **开启融合时**：`LegalPromptContextBuilder.build` 额外增加 100–500 ms（三源检索串行/并行 IO），成为新的首要瓶颈。

## 性能基线记录表

> 以下预测值基于代码路径分析、本地 mock 压测与同类服务经验估算，**非生产实测数据**。正式环境需按「实际执行步骤」重新采集。

### 1. 低并发基线 — 10 并发用户

> 验证服务在轻量负载下的响应能力与稳定性。

| 指标 | 综合 | OA 解读 | 创造性分析 | 新颖性分析 |
|---|---|---|---|---|
| **QPS** | 45 | 55 | 35 | 30 |
| **P50 (ms)** | 95 | 85 | 105 | 120 |
| **P95 (ms)** | 180 | 160 | 200 | 240 |
| **P99 (ms)** | 350 | 300 | 380 | 450 |
| **错误率 (%)** | <0.1 | <0.1 | <0.1 | <0.1 |
| **CPU 使用率 (%)** | 25 | — | — | — |
| **内存使用 (MB)** | 512 | — | — | — |
| **缓存命中率 (%)** | 65 | — | — | — |
| **LLM 平均延迟 (ms)** | N/A（本链路不直接调用 LLM） | — | — | — |

### 2. 中等并发基线 — 50 并发用户

> 验证服务在中等压力下的吞吐量与资源瓶颈。

| 指标 | 综合 | OA 解读 | 创造性分析 | 新颖性分析 |
|---|---|---|---|---|
| **QPS** | 72 | 85 | 60 | 50 |
| **P50 (ms)** | 140 | 120 | 160 | 190 |
| **P95 (ms)** | 320 | 280 | 350 | 420 |
| **P99 (ms)** | 580 | 500 | 620 | 750 |
| **错误率 (%)** | <0.5 | <0.5 | <0.5 | <0.5 |
| **CPU 使用率 (%)** | 55 | — | — | — |
| **内存使用 (MB)** | 780 | — | — | — |
| **缓存命中率 (%)** | 72 | — | — | — |
| **LLM 平均延迟 (ms)** | N/A | — | — | — |

### 3. 高并发基线 — 100 并发用户

> 验证服务在接近容量上限时的表现，识别降级拐点。

| 指标 | 综合 | OA 解读 | 创造性分析 | 新颖性分析 |
|---|---|---|---|---|
| **QPS** | 88 | 100 | 75 | 65 |
| **P50 (ms)** | 220 | 190 | 250 | 300 |
| **P95 (ms)** | 650 | 580 | 700 | 850 |
| **P99 (ms)** | 1200 | 1050 | 1300 | 1600 |
| **错误率 (%)** | <2 | <2 | <2 | <2 |
| **CPU 使用率 (%)** | 82 | — | — | — |
| **内存使用 (MB)** | 1100 | — | — | — |
| **缓存命中率 (%)** | 78 | — | — | — |
| **LLM 平均延迟 (ms)** | N/A | — | — | — |

> **容量拐点预估**：当并发 > 120 或 QPS > 100 时，Neo4j 连接池与 BGE-M3 CPU 竞争将成为硬瓶颈，P99 可能快速劣化至 >3s。

## 性能趋势图

```
QPS
 │
 │         ┌─ 目标容量线 (100 QPS)
 │        ╱
 │    ╭──╯    ╭─ 实际预测曲线
 │   ╱        ╱
 │  ╱    ╭───╯
 │ ╱    ╱
 └───────────────────────────> 并发用户
      10    50    100

延迟 (ms)
 │
 │                    P99 ─╮
 │               P95 ─╮    │
 │          P50 ─╮    │    │
 │    ╭──────────╯    │    │
 │   ╱    ╭───────────╯    │
 │  ╱    ╱    ╭────────────╯
 └───────────────────────────> 并发用户
      10    50    100
```

## 关键结论与行动项

| # | 发现 | 优先级 | 行动项 | 负责人 | 截止日期 |
|---|---|---|---|---|---|
| 1 | 场景识别（BGE-M3）为 CPU 首要瓶颈，占 35% 耗时 | P0 | 引入意图识别结果缓存（TTL 10 min），对高频 query 直接命中 | 研发 | 2026-05-07 |
| 2 | 规则检索（Neo4j）在高并发下连接池易耗尽 | P1 | 热点规则本地 LRU 缓存（max_size=500, TTL 300s）+ 连接池扩容 | 研发 | 2026-05-14 |
| 3 | 融合构建（三源检索）在开启时延迟增加 100–500 ms | P1 | 证据检索异步并行化 + 预加载高频领域证据 | 研发 | 2026-05-14 |
| 4 | 缓存命中率在 100 并发时仅 78%，仍有提升空间 | P2 | 实施「预加载高频提示词」策略（见 runbook 附录） | 研发 | 2026-05-21 |
| 5 | 变量治理阶段 regex 与 schema 构造重复开销 | P2 | schema 预编译（启动时加载）+ regex pattern 缓存 | 研发 | 2026-05-21 |

## 附录

### 压测执行命令

```bash
# 10 用户基线
./scripts/run_load_test.sh http://localhost:8000 10 5m

# 50 用户基线
./scripts/run_load_test.sh http://localhost:8000 50 10m

# 100 用户基线
./scripts/run_load_test.sh http://localhost:8000 100 10m

# 带性能分析（生成 cProfile 报告）
python3 scripts/profile_generate_prompt.py
```

### 缓存优化建议摘要

1. **意图识别缓存**：对 `user_input` 做 SHA256 哈希，缓存 `ScenarioIdentifierOptimized` 输出（TTL 600s）。
2. **规则缓存**：对 `domain+task_type` 组合缓存 `ScenarioRule` 对象（TTL 300s），避免重复 Neo4j 查询。
3. **提示词模板缓存**：已存在 `PromptTemplateCache`，建议将 `max_size` 从 1000 提升至 2000，并启用启动预加载。
4. **预加载策略**：服务启动时，读取 `config/prompt_fusion_rollout.yaml` 中高频 domain/task_type 组合，预热缓存至 80% 命中率。

### 历史记录

| 日期 | 版本 | 场景 | 执行人 | 报告路径 |
|---|---|---|---|---|
| 2026-04-23 | v2.1.0 | 代码级基线预测 + cProfile 分析 | Agent-PerfObs | `reports/performance_profile.txt` |
