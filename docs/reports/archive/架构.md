import matplotlib.pyplot as plt
import matplotlib.patches as mpatches
from matplotlib.patches import FancyBboxPatch, FancyArrowPatch, Circle, Wedge
import numpy as np

fig, ax = plt.subplots(figsize=(22, 16))
ax.set_xlim(0, 22)
ax.set_ylim(0, 16)
ax.axis('off')

# 颜色定义 - 专利业务专用配色
colors = {
    'gateway': '#1A237E',      # 深蓝 - 统一网关
    'gateway_bg': '#E8EAF6',
    'autogen': '#4A148C',      # 深紫 - AutoGen编排
    'autogen_bg': '#F3E5F5',
    'agent_planner': '#1565C0', # 规划器
    'agent_retriever': '#2E7D32', # 检索
    'agent_analyzer': '#E65100', # 分析
    'agent_writer': '#6A1B9A', # 撰写
    'agent_rule': '#C62828',   # 规则
    'agent_polish': '#00695C', # 润色
    'agent_bg': '#E3F2FD',
    'shared': '#FF8F00',       # 共享服务
    'shared_bg': '#FFF8E1',
    'tools': '#2E7D32',        # 工具
    'tools_bg': '#E8F5E9',
    'infra': '#455A64',        # 基础设施
    'infra_bg': '#ECEFF1',
    'border': '#263238',
    'text': '#212121',
    'light_text': '#FFFFFF'
}

def draw_box(ax, x, y, w, h, text, facecolor, edgecolor, fontsize=9, 
             text_color='#212121', bold=True, radius=0.15, lw=2):
    box = FancyBboxPatch((x, y), w, h, boxstyle=f"round,pad=0.02,rounding_size={radius}",
                         facecolor=facecolor, edgecolor=edgecolor, linewidth=lw)
    ax.add_patch(box)
    weight = 'bold' if bold else 'normal'
    ax.text(x + w/2, y + h/2, text, ha='center', va='center', fontsize=fontsize,
            color=text_color, weight=weight, wrap=True, linespacing=1.3)
    return box

def draw_round_box(ax, x, y, w, h, text, facecolor, edgecolor, fontsize=10, 
                   text_color='#212121', bold=True, lw=2.5):
    box = FancyBboxPatch((x, y), w, h, boxstyle="round,pad=0.03,rounding_size=0.4",
                         facecolor=facecolor, edgecolor=edgecolor, linewidth=lw)
    ax.add_patch(box)
    weight = 'bold' if bold else 'normal'
    ax.text(x + w/2, y + h/2, text, ha='center', va='center', fontsize=fontsize,
            color=text_color, weight=weight, wrap=True, linespacing=1.3)
    return box

def draw_arrow(ax, start, end, color='#90A4AE', lw=1.5, style='->', rad=0):
    ax.annotate('', xy=end, xytext=start,
                arrowprops=dict(arrowstyle=style, color=color, lw=lw,
                              connectionstyle=f"arc3,rad={rad}"))

# ==================== 标题 ====================
ax.text(11, 15.6, '专利多智能体系统架构（基于 AutoGen 编排）', ha='center', va='center',
        fontsize=20, weight='bold', color=colors['border'])
ax.text(11, 15.15, '专利业务单元智能体化 + 统一网关 + 知识库/记忆/NLP 共享服务', ha='center', va='center',
        fontsize=12, color='#607D8B')

# ==================== 第1层：统一网关 ====================
gateway_box = draw_round_box(ax, 3, 13.8, 16, 1.0, 
    '统一网关（Unified Gateway）\nREST API / WebSocket / CLI / WebHook  |  鉴权 / 限流 / 路由 / 负载均衡',
    colors['gateway_bg'], colors['gateway'], fontsize=11, text_color=colors['gateway'])

ax.text(0.5, 14.3, '入口层', fontsize=11, weight='bold', color=colors['gateway'], va='center')

# ==================== 第2层：AutoGen 编排层 ====================
autogen_box = draw_round_box(ax, 2.5, 11.5, 17, 1.8, '', colors['autogen_bg'], colors['autogen'], lw=2.5)
ax.text(11, 12.95, 'AutoGen 编排层（Orchestration Layer）', ha='center', va='center',
        fontsize=13, weight='bold', color=colors['autogen'])

# AutoGen 内部组件
autogen_comps = [
    (3.0, 11.7, 2.8, 0.9, 'GroupChat\n群聊管理', '#E1BEE7'),
    (6.2, 11.7, 2.8, 0.9, 'Selector\n智能体选择器', '#E1BEE7'),
    (9.4, 11.7, 2.8, 0.9, 'MessageQueue\n消息队列', '#E1BEE7'),
    (12.6, 11.7, 2.8, 0.9, 'StateManager\n状态管理', '#E1BEE7'),
    (15.8, 11.7, 2.8, 0.9, 'Termination\n终止条件', '#E1BEE7'),
]
for x, y, w, h, text, bg in autogen_comps:
    draw_box(ax, x, y, w, h, text, bg, colors['autogen'], fontsize=9, text_color=colors['autogen'], radius=0.1)

ax.text(0.5, 12.4, '编排层', fontsize=11, weight='bold', color=colors['autogen'], va='center')

# ==================== 第3层：专利业务智能体层 ====================
agent_y = 9.2
agent_h = 1.6
agent_w = 2.6

agents = [
    (2.0, 'Planner\n规划器', colors['agent_planner'], 
     '任务拆解\n策略制定\n工作流编排'),
    (5.0, 'Retriever\n检索智能体', colors['agent_retriever'], 
     '专利数据库检索\n关键词扩展\n对比文件筛选'),
    (8.0, 'Analyzer\n分析智能体', colors['agent_analyzer'], 
     '技术特征比对\n新颖性分析\n侵权分析'),
    (11.0, 'Writer\n撰写智能体', colors['agent_writer'], 
     '权利要求书\n说明书\n审查意见答复'),
    (14.0, 'RuleAgent\n规则智能体', colors['agent_rule'], 
     '法律规则校验\n形式审查\n合规性检查'),
    (17.0, 'Polisher\n润色智能体', colors['agent_polish'], 
     '语言润色\n格式标准化\n最终质检'),
]

for i, (x, title, color, desc) in enumerate(agents):
    # 主框
    box = FancyBboxPatch((x, agent_y), agent_w, agent_h, 
                         boxstyle="round,pad=0.02,rounding_size=0.2",
                         facecolor='#FFFFFF', edgecolor=color, linewidth=2.5)
    ax.add_patch(box)
    # 标题条
    title_box = FancyBboxPatch((x, agent_y + agent_h - 0.45), agent_w, 0.45,
                               boxstyle="round,pad=0.02,rounding_size=0.1",
                               facecolor=color, edgecolor=color, linewidth=0)
    ax.add_patch(title_box)
    ax.text(x + agent_w/2, agent_y + agent_h - 0.22, title, ha='center', va='center',
            fontsize=10, weight='bold', color='white')
    # 描述
    ax.text(x + agent_w/2, agent_y + 0.5, desc, ha='center', va='center',
            fontsize=8, color=colors['text'], linespacing=1.4)

ax.text(0.5, 10.0, '智能体层', fontsize=11, weight='bold', color=colors['border'], va='center')

# 智能体之间的横向连接（表示它们通过AutoGen通信）
for i in range(len(agents)-1):
    x1 = agents[i][0] + agent_w
    x2 = agents[i+1][0]
    ax.annotate('', xy=(x2, agent_y + agent_h/2), xytext=(x1, agent_y + agent_h/2),
                arrowprops=dict(arrowstyle='<->', color='#BDBDBD', lw=1.2,
                              connectionstyle="arc3,rad=0"))

# ==================== 第4层：共享服务层（知识库/记忆/NLP）====================
shared_y = 6.3
shared_h = 2.4
shared_w = 5.0

# 知识库
kb_box = draw_round_box(ax, 1.5, shared_y, shared_w, shared_h, '', colors['shared_bg'], colors['shared'], lw=2)
ax.text(4.0, shared_y + shared_h - 0.3, '知识库系统（Knowledge Base）', ha='center', va='center',
        fontsize=11, weight='bold', color=colors['shared'])
kb_items = ['专利全文库（7500万）', '法律世界模型', '无效宣告决定书（3万）', '判决书知识图谱（6000）']
for i, item in enumerate(kb_items):
    ax.text(4.0, shared_y + shared_h - 0.75 - i*0.45, f'• {item}', ha='center', va='center',
            fontsize=8.5, color=colors['text'])

# 记忆系统
mem_box = draw_round_box(ax, 7.5, shared_y, shared_w, shared_h, '', colors['shared_bg'], colors['shared'], lw=2)
ax.text(10.0, shared_y + shared_h - 0.3, '记忆系统（Memory System）', ha='center', va='center',
        fontsize=11, weight='bold', color=colors['shared'])
mem_items = ['Hot Memory（当前会话）', 'Warm Memory（近期上下文）', 'Cold Memory（长期档案）', 'Auto-Compact 压缩']
for i, item in enumerate(mem_items):
    ax.text(10.0, shared_y + shared_h - 0.75 - i*0.45, f'• {item}', ha='center', va='center',
            fontsize=8.5, color=colors['text'])

# NLP系统
nlp_box = draw_round_box(ax, 13.5, shared_y, shared_w, shared_h, '', colors['shared_bg'], colors['shared'], lw=2)
ax.text(16.0, shared_y + shared_h - 0.3, 'NLP 服务层', ha='center', va='center',
        fontsize=11, weight='bold', color=colors['shared'])
nlp_items = ['文本分块 / 嵌入（BGE-m3）', '实体抽取 / 关系识别', '专利IPC分类预测', '摘要生成 / 关键词提取']
for i, item in enumerate(nlp_items):
    ax.text(16.0, shared_y + shared_h - 0.75 - i*0.45, f'• {item}', ha='center', va='center',
            fontsize=8.5, color=colors['text'])

ax.text(0.5, 7.5, '共享服务层', fontsize=11, weight='bold', color=colors['shared'], va='center')

# ==================== 第5层：工具层 ====================
tools_y = 3.8
tools_h = 2.0
tools_w = 3.5

tools = [
    (1.5, '文档解析工具', colors['tools_bg'], colors['tools'],
     'MinerU / Docling\nPDF/Word 结构化提取'),
    (5.5, '数据库连接器', colors['tools_bg'], colors['tools'],
     'Qdrant 向量检索\nNeo4j 图查询'),
    (9.5, '专利数据源', colors['tools_bg'], colors['tools'],
     'CNIPA / WIPO / EPO\n商业专利数据库 API'),
    (13.5, '代码/脚本工具', colors['tools_bg'], colors['tools'],
     'Python 执行\nShell / Git 操作'),
    (17.0, '外部服务', colors['tools_bg'], colors['tools'],
     'N8N 工作流触发\n邮件 / 通知推送'),
]

for x, title, bg, edge, desc in tools:
    box = FancyBboxPatch((x, tools_y), tools_w, tools_h,
                         boxstyle="round,pad=0.02,rounding_size=0.15",
                         facecolor=bg, edgecolor=edge, linewidth=1.8)
    ax.add_patch(box)
    ax.text(x + tools_w/2, tools_y + tools_h - 0.3, title, ha='center', va='center',
            fontsize=10, weight='bold', color=edge)
    ax.text(x + tools_w/2, tools_y + 0.6, desc, ha='center', va='center',
            fontsize=8, color=colors['text'], linespacing=1.3)

ax.text(0.5, 4.8, '工具层', fontsize=11, weight='bold', color=colors['tools'], va='center')

# ==================== 第6层：基础设施层 ====================
infra_y = 1.2
infra_h = 1.8
infra_w = 3.8

infra_items = [
    (1.2, '模型 Provider', colors['infra_bg'], colors['infra'],
     'Kimi API (K2.5)\nDeepSeek / Qwen\noMLX / MLX 本地推理'),
    (5.5, '向量存储', colors['infra_bg'], colors['infra'],
     'Qdrant 向量数据库\nBGE-m3 嵌入模型\n语义检索服务'),
    (9.8, '图数据库', colors['infra_bg'], colors['infra'],
     'Neo4j 知识图谱\n专利关系网络\n无效宣告关联'),
    (14.1, '监控与可观测', colors['infra_bg'], colors['infra'],
     'Prometheus + Alertmanager\nMetrics 端点\n分布式追踪'),
]

for x, title, bg, edge, desc in infra_items:
    box = FancyBboxPatch((x, infra_y), infra_w, infra_h,
                         boxstyle="round,pad=0.02,rounding_size=0.15",
                         facecolor=bg, edgecolor=edge, linewidth=1.8)
    ax.add_patch(box)
    ax.text(x + infra_w/2, infra_y + infra_h - 0.3, title, ha='center', va='center',
            fontsize=10, weight='bold', color=edge)
    ax.text(x + infra_w/2, infra_y + 0.55, desc, ha='center', va='center',
            fontsize=8, color=colors['text'], linespacing=1.3)

ax.text(0.5, 2.1, '基础设施层', fontsize=11, weight='bold', color=colors['infra'], va='center')

# ==================== 连接线 ====================

# 网关 -> AutoGen
ax.annotate('', xy=(11, 13.3), xytext=(11, 13.8),
            arrowprops=dict(arrowstyle='->', color=colors['gateway'], lw=2.5))

# AutoGen -> 各智能体（扇形分发）
for i, (x, _, _, _) in enumerate(agents):
    agent_center_x = x + agent_w/2
    ax.annotate('', xy=(agent_center_x, agent_y + agent_h), xytext=(agent_center_x, 11.5),
                arrowprops=dict(arrowstyle='<->', color=colors['autogen'], lw=1.5,
                              connectionstyle="arc3,rad=0"))

# 智能体 -> 共享服务层
for i, (x, _, _, _) in enumerate(agents):
    agent_center_x = x + agent_w/2
    # 连接到知识库
    if i < 3:
        ax.annotate('', xy=(4.0, shared_y + shared_h), xytext=(agent_center_x, agent_y),
                    arrowprops=dict(arrowstyle='<->', color=colors['shared'], lw=1, alpha=0.5,
                                  connectionstyle="arc3,rad=-0.1"))
    # 连接到记忆
    ax.annotate('', xy=(10.0, shared_y + shared_h), xytext=(agent_center_x, agent_y),
                arrowprops=dict(arrowstyle='<->', color=colors['shared'], lw=1, alpha=0.5,
                              connectionstyle="arc3,rad=0"))
    # 连接到NLP
    if i >= 2:
        ax.annotate('', xy=(16.0, shared_y + shared_h), xytext=(agent_center_x, agent_y),
                    arrowprops=dict(arrowstyle='<->', color=colors['shared'], lw=1, alpha=0.5,
                                  connectionstyle="arc3,rad=0.1"))

# 共享服务层 -> 工具层
ax.annotate('', xy=(4.0, tools_y + tools_h), xytext=(4.0, shared_y),
            arrowprops=dict(arrowstyle='<->', color=colors['tools'], lw=1.5))
ax.annotate('', xy=(10.0, tools_y + tools_h), xytext=(10.0, shared_y),
            arrowprops=dict(arrowstyle='<->', color=colors['tools'], lw=1.5))
ax.annotate('', xy=(16.0, tools_y + tools_h), xytext=(16.0, shared_y),
            arrowprops=dict(arrowstyle='<->', color=colors['tools'], lw=1.5))

# 工具层 -> 基础设施层
ax.annotate('', xy=(3.1, infra_y + infra_h), xytext=(3.25, tools_y),
            arrowprops=dict(arrowstyle='<->', color=colors['infra'], lw=1.5))
ax.annotate('', xy=(7.4, infra_y + infra_h), xytext=(7.25, tools_y),
            arrowprops=dict(arrowstyle='<->', color=colors['infra'], lw=1.5))
ax.annotate('', xy=(11.7, infra_y + infra_h), xytext=(11.25, tools_y),
            arrowprops=dict(arrowstyle='<->', color=colors['infra'], lw=1.5))
ax.annotate('', xy=(16.0, infra_y + infra_h), xytext=(15.75, tools_y),
            arrowprops=dict(arrowstyle='<->', color=colors['infra'], lw=1.5))

# ==================== 右侧图例/说明 ====================
legend_x = 20.2
ax.text(legend_x, 14.5, '数据流向说明', fontsize=10, weight='bold', color=colors['border'])
legend_items = [
    (14.0, colors['gateway'], '用户请求流'),
    (13.0, colors['autogen'], '编排控制流'),
    (12.0, colors['shared'], '共享服务调用'),
    (11.0, colors['tools'], '工具执行流'),
    (10.0, colors['infra'], '基础设施层'),
]
for y, color, label in legend_items:
    ax.plot([legend_x - 0.3, legend_x + 0.3], [y, y], color=color, lw=3, solid_capstyle='round')
    ax.text(legend_x + 0.5, y, label, fontsize=9, color=colors['text'], va='center')

# 添加工作流示例标注
workflow_box = FancyBboxPatch((19.5, 5.5), 2.2, 3.5, boxstyle="round,pad=0.02,rounding_size=0.1",
                              facecolor='#FAFAFA', edgecolor='#BDBDBD', linewidth=1, linestyle='--')
ax.add_patch(workflow_box)
ax.text(20.6, 8.7, '典型工作流', fontsize=10, weight='bold', color=colors['border'], ha='center')
workflow_steps = [
    '1. 用户提交检索任务',
    '2. Planner 拆解子任务',
    '3. Retriever 检索专利',
    '4. Analyzer 技术比对',
    '5. RuleAgent 合规校验',
    '6. Writer 生成报告',
    '7. Polisher 润色输出',
]
for i, step in enumerate(workflow_steps):
    ax.text(20.6, 8.2 - i*0.4, step, fontsize=7.5, color=colors['text'], ha='center')

plt.tight_layout()
plt.savefig('/mnt/agents/output/patent_multi_agent_architecture.png', dpi=200, bbox_inches='tight',
            facecolor='white', edgecolor='none')
plt.show()
print("专利多智能体架构图已保存")