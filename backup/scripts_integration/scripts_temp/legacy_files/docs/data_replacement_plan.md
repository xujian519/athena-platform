# 专利数据替换方案

## 📊 数据情况分析

- **当前数据**: 2800万条记录（来自5700万原始数据的子集）
- **原始数据**: 5700万条完整记录
- **数据格式**: CSV文件，90GB
- **重复情况**: 100%重复（现有数据完全包含在原始数据中）

## 🎯 替换策略

### 方案选择：完全替换
由于现有2800万条数据是5700万原始数据的子集，最有效的方案是：
1. **备份现有数据**（可选）
2. **清空patents表**
3. **批量导入5700万条原始数据**
4. **重建索引和统计信息**

### 优势分析
- ✅ **避免重复检查**：无需逐条比对，效率最高
- ✅ **数据完整性**：确保使用最新的完整数据
- ✅ **性能优化**：清空表后批量插入，速度最快
- ✅ **索引优化**：一次性重建所有索引

## 🛠️ 执行步骤

### 第1步：数据准备
```bash
# 确认数据路径
ls -la /path/to/csv/files/
# 应该包含多个CSV文件，总计约90GB
```

### 第2步：执行替换
```bash
# 使用替换脚本
python3 scripts/replace_patent_data.py /path/to/csv/directory
```

### 第3步：验证结果
```sql
-- 检查记录数
SELECT COUNT(*) FROM patents;

-- 检查数据质量
SELECT
    COUNT(*) as total,
    COUNT(CASE WHEN publication_number != '' THEN 1 END) as has_pub_num,
    COUNT(CASE WHEN application_number != '' THEN 1 END) as has_app_num
FROM patents;
```

## ⚡ 性能优化

### 1. 批量处理优化
- **批量大小**: 10,000条记录/批
- **内存控制**: 分批读取，避免内存溢出
- **事务管理**: 每批独立提交

### 2. 索引策略
- **导入期间**: 暂时删除索引
- **导入完成**: 一次性重建所有索引
- **索引类型**: B-tree索引 + GIN全文索引

### 3. 空间预估
- **清理前**: 200GB（2800万条）
- **清理后**: ~0GB（表已清空）
- **导入后**: 预计350-400GB（5700万条）

## 📈 预期效果

### 数据质量
- ✅ 完整的5700万条专利记录
- ✅ 无重复数据
- ✅ 统一的数据格式

### 性能指标
- **导入速度**: 20,000-50,000条/秒
- **总耗时**: 约20-30小时
- **峰值内存**: <4GB

### 存储优化
- **数据压缩**: 使用PostgreSQL TOAST
- **索引优化**: 合理的索引策略
- **统计信息**: 更新的表统计信息

## 🔍 监控命令

### 导入进度监控
```bash
# 查看日志
tail -f /tmp/patent_replace.log

# 监控数据库大小
watch -n 300 'psql -h localhost -U postgres -d patent_db -c "SELECT COUNT(*), pg_size_pretty(pg_total_relation_size('\''patents'\'')) FROM patents;"'

# 监控系统资源
htop
iostat -x 1
```

### 性能调优建议
```sql
-- PostgreSQL配置优化
SET maintenance_work_mem = '2GB';
SET checkpoint_completion_target = 0.9;
SET wal_buffers = '64MB';
```

## ⚠️ 风险控制

### 1. 备份策略
- 自动创建备份表（可选）
- 记录操作日志
- 支持中断恢复

### 2. 错误处理
- 完整的异常捕获
- 详细的错误日志
- 批量回滚机制

### 3. 验证机制
- 记录数验证
- 数据质量检查
- 索引完整性验证

## 🚀 开始执行

确认执行后，将：
1. 清空现有2800万条记录
2. 导入5700万条新记录
3. 重建所有索引
4. 优化数据库性能

**预计总时间**: 20-30小时
**预计最终大小**: 350-400GB
**数据完整性**: 100%